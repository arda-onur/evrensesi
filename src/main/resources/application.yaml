spring:
  application:
    name: evrensesi

  profiles:
    active: dev

server:
  port: 8080

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: never

  messages:
    basename: ExceptionMessages,ValidationMessages
    encoding: UTF-8
